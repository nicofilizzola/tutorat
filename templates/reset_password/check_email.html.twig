{% extends 'base.html.twig' %}

{% block body %}
    <p>S'il existe bien un compte avec l'adresse email indiqué, vous recevrez un mail qui contient un lien vous permettant de réinitialiser votre mot de passe.</p>
    <p>Ce lien expirera après une heure.</p>
    <p>Si vous n'avez pas reçu le mail, veuillez vérifier dans vos courriers indésirables (boîte spam). Sinon, vous pouvez toujours <a href="{{ path('app_forgot_password_request') }}">reésayer</a>.</p>
    {# <p>
        If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.
        This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </p> #}
{% endblock %}
